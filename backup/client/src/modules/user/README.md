# User Module (Client)

Handles user authentication UI and account management.

## Components
- `UserMenu.vue`: Header component for login status, sign out, and account switching.

## State Management
- Uses global `user` and `activeAccount` state (passed via props or shared store).
