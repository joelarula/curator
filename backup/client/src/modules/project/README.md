# Project Module (Client)

Handles project selection, creation, and metadata display.

## Components
- `ProjectMenu.vue`: Toolbar component for folder selection and project management.
- `ProjectIndicator.vue`: Small chip/badge showing the currently active project.
- `CreateProjectDialog.vue`: Modal for new project creation.

## State Management
- Track `currentProject` and `projectsList`.
