# MCP Module

Encapsulates MCP (Model Context Protocol) server logic.

## Components
- `mcp.service.ts`: The core MCP server implementation, exposing tools like `consult`.

## Interaction
- This module depends on the `Consultant` module for semantic search capabilities.
